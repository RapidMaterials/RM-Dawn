function submitToZapier(
  actionValue,
  modificationsValue
) {

  const webhook =
    'https://hooks.zapier.com/hooks/catch/21037350/4hmzwbn/';


  const zapParams =
    new URLSearchParams();


  zapParams.set(
    'action',
    actionValue
  );


  zapParams.set(
    'order_number',
    order
  );


  if (modificationsValue) {

    zapParams.set(
      'modifications',
      modificationsValue
    );

  }


  /*
    Make every request unique so the browser
    cannot cache/reuse an earlier iframe request.
  */

  zapParams.set(
    'timestamp',
    Date.now().toString()
  );


  const webhookUrl =
    webhook +
    '?' +
    zapParams.toString();


  console.log(
    'Sending to Zapier:',
    webhookUrl
  );


  const iframe =
    document.getElementById(
      'zapier-response'
    );


  /*
    Clear the iframe first, then load the
    unique Zapier URL.
  */

  iframe.src =
    'about:blank';


  setTimeout(function () {

    iframe.src =
      webhookUrl;

  }, 50);

}